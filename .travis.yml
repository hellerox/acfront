os: linux
language: go
go:
  - "1.14rc1"
  - tip
branches:
  only:
    - master
env:
  global:
    - GOLANGCILINT_VERSION='v1.23.6'
install:
  - echo "Install will do nothing"
script:
  - GOOS=linux GOARCH=amd64 make lint test
after_failure:
  - bash <(curl -s https://codecov.io/bash)
after_success:
  - reports-publisher
  - bash <(curl -s https://codecov.io/bash)